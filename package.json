{
  "name": "dashboard-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "xlsx": "^0.18.5",
    "react-icons": "^4.12.0",
    "react-router-dom": "^6.8.0"
  },
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider yarn react-scripts start",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider yarn react-scripts build",
    "test": "cross-env NODE_OPTIONS=--openssl-legacy-provider yarn react-scripts test",
    "eject": "yarn react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "react-scripts": "5.0.1",
    "cross-env": "^7.0.3"
  }
}